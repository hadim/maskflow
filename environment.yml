# Usqge: `conda env update -f environment.yml`
channels:
  - conda-forge

dependencies:
    - python>=3.6
    - numpy
    - scipy
    - matplotlib
    - pillow
    - scikit-image
    - scikit-learn
    - tifffile
    - scipy
    - opencv
    - pycocotools
    - tqdm
    - jupyterlab
    - pytest
    - pandas
    - pyyaml
    - pylint
    - mypy
    - importlib_resources
    - pep8
    - requests
    - ipywidgets
    # Tensorflow dependencies
    - protobuf
    - absl-py
    - gast
    - astor
    - termcolor
    - h5py
    - wrapt
    # Tensorboard dependencies
    - werkzeug
    - markdown
    - grpcio
    # Tensorflow datasets dependencies
    - promise
    - dill
    #- pip:
        #- pip install --no-deps -U keras_preprocessing keras_applications
        #- pip install --no-deps -U tensorflow-estimator tensorflow-datasets tensorflow_metadata
        #- pip install --no-deps -U tensorflow==2.0.0-alpha0 tensorboard
        # or
        #- pip install --no-deps -U tensorflow-gpu==2.0.0-alpha0 tensorboard
        # or
        #- pip install --no-deps -U tf-nightly-cpu-2.0-preview tb-nightly
        # or
        #- pip install --no-deps -U tf-nightly-2.0-preview tb-nightly
